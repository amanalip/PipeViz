{
  "//": [
    "Package name matches the repository and GitHub Pages project path.",
    "Private prevents accidental npm publish; PipeViz is an app, not a library.",
    "ESM everywhere: Vite 8, ESLint 10 flat config, and TS 7 all assume module type.",
    "All versions are exact-pinned (no carets) per project_plan.md sections 4 and 15:",
    "upgrades are deliberate and checked against changelogs.",
    "TypeScript is pinned to 5.9.3 per the plan's documented fallback: typescript-eslint",
    "8.x peers on typescript <6.1.0, so the TS 7 native line cannot install cleanly yet.",
    "Scripts mirror README section 6: dev, test, build, lint (+ preview, typecheck extras)."
  ],
  "name": "pipeviz",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Paste a Jenkinsfile. See your pipeline. Client-side Jenkins pipeline visualizer.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@codemirror/commands": "6.11.0",
    "@codemirror/language": "6.12.4",
    "@codemirror/legacy-modes": "6.5.3",
    "@codemirror/state": "6.7.1",
    "@codemirror/view": "6.43.9",
    "@lezer/highlight": "1.2.3",
    "@xyflow/react": "12.11.3",
    "html-to-image": "1.11.13",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "6.1.0",
    "eslint": "10.9.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.4",
    "globals": "17.11.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.67.0",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
